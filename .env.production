# Vercel Environment Variables (Production)
# VITE_API_URL will be set automatically by Vercel
VITE_APP_NAME=Velocity Zones Dashboard
VITE_APP_VERSION=1.0.0
VITE_ENVIRONMENT=production

# API Configuration (Production)
NODE_ENV=production
# CORS_ORIGIN will be set automatically by Vercel
LOG_LEVEL=warn

# Note: Using in-memory storage for Vercel serverless deployment
# Data will be initialized with default values on each cold start